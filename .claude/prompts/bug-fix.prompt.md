Find all bugs in the data pipeline layer (silver and gold pipelines). For each bug:

1) Write a failing test that reproduces it,
2) Fix the code,
3) Run pytest and iterate until all tests pass. Also run the existing test suite to ensure no regressions. Work autonomously without asking me questions—use your best judgment and document any assumptions in code comments.
