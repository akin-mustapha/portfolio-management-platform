# Improvement

While project development is temporary on hold. User will continue ot make use of app. Any improvments, bugs, ui/ux issue should be logged here.

## Code Improvement

**frontend:**

Ui component should be extract from callback, helpers.
example `_chart_card` in `_helper.py` this is a ui component and should be stored in the components tab.

Close button on `asset detail card`, user has to uncheck checkbox on `asset table` before user can remove asset from view

`drawdown chart` make `y` axis `percentage`. or Add a toggle button

## Outstanding

* Industry and Sector information
* ETF Holdings
* Correlation Analysis
