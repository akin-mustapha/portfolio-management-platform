# Features

1. Asset table currently has a option menue to display or hide a column on the table, currently the menue doesn't work. Recommend fix. Done when user can hide or display a column and move the columns to reorder then

2. Hardcode the asset industry and sector, Create a new migration

3. Add ui form to add a tag.